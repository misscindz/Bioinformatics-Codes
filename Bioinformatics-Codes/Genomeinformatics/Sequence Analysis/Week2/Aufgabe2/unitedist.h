#ifndef UNITEDIST_H
#define UNITEDIST_H

unsigned long unitedist(const unsigned char *useq,
                        unsigned long ulen,
                        const unsigned char *vseq,
                        unsigned long vlen);

#endif
